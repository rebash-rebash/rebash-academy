direction: right

EDIT: "Edit YAML locally"
    DIFF: "kubectl diff -f ."
    APPLY: "kubectl apply -f ."
    VERIFY: "kubectl rollout status"
    GIT: "git commit + push"
    EDIT -> DIFF
    DIFF -> APPLY
    APPLY -> VERIFY
    VERIFY -> GIT
