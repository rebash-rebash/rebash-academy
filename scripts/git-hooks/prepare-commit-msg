#!/bin/sh
# Strip Co-authored-by trailers before the commit message is finalized.
# Installed via: bash scripts/install-git-hooks.sh

MSG_FILE=$1

grep -vi '^co-authored-by:' "$MSG_FILE" > "${MSG_FILE}.tmp"
mv "${MSG_FILE}.tmp" "$MSG_FILE"
