#!/bin/sh
# Final pass: strip any Cursor co-author trailer before the commit is recorded.
# Installed via: bash scripts/install-git-hooks.sh

MSG_FILE=$1

grep -viE 'co-authored-by:.*cursor|cursoragent@cursor\.com' "$MSG_FILE" > "${MSG_FILE}.tmp"
mv "${MSG_FILE}.tmp" "$MSG_FILE"
